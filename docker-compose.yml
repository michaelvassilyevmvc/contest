services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres_contest
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgrespw
      - POSTGRES_DB=contestdb
    ports:
      - 5435:5432
    volumes:
      - ./pgdata:/var/lib/postgresql/data
volumes:
  pgdata: